package ide;

/**
 * Выполнить 5 кат
 */
public class IdeKata {
    /**
     * 1. Запустить через `Shift` + `F10`
     * 2. Поставить Break Point через `Ctrl` + `F8`
     * 3. Запустить через `Shift` + `F9`
     */
    public static void main(String[] args) {
        int a = 0;
        long b = 1L;

        // 4.
        // вызвать `substitutionsKata`
        // для подстановки подходящего параметра использовать `Ctrl` + `Shift` + `Space`


    }

    private static void substitutionsKata(long value) {
        // 5.
        // Завернуть в try-catch используя `Ctrl` + `Alt` + `T`
        System.out.println(value);
    }
}
